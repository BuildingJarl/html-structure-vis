'use babel';

import HtmlStructureVisView from '../lib/html-structure-vis-view';

describe('HtmlStructureVisView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
